# CLAUDE.md

## 前端

项目技术栈为：React + tailwindcss + TypeScript + vite + antd + axios + zustand + react-router

每次执行任务后都需要检查是否有eslint、typescript错误提示

## 后端

后端技术栈为：python + fastapi + MongoDB
开发环境已安装MongoDB, 端口为27017
